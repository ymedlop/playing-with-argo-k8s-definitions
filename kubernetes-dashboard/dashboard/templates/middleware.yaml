apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: kubernetes-dashboard-forward-auth
spec:
  forwardAuth:
    address: http://kubernetes-dashboard-forward-auth:4181
    authResponseHeaders:
      - X-Forwarded-User