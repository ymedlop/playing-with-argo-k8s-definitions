apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kubernetes-dashboard-localhost
spec:
  commonName: kubernetes-dashboard.localhost.com
  secretName: kubernetes-dashboard-localhost
  dnsNames:
    - kubernetes-dashboard.localhost.com
  issuerRef:
    name: letsencrypt-traefik2
    kind: ClusterIssuer